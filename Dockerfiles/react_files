# pull official base image
FROM node:16.15.0-alpine

# set working directory
WORKDIR /app

# add app
COPY ./ ./

# install app dependencies
RUN npm install -g yarn lerna --force

RUN lerna bootstrap --no-ci --use-workspaces

# start app
CMD ["yarn", "run", "start:main"]